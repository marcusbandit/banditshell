# TODO

One step at a time. Each item gets looked at, understood, then handled —
not batched. Update this file as work happens: notes, decisions, and
done-marks go under the item they belong to.

- [ ] Settings menu
- [ ] Peak
- [ ] Media Viewer
  - 2026-09-24: empty state reworked — no MPRIS player at all now keeps the
    card's track layout in ghost tones (art square, "nothing is playing", a
    line pointing at players that would register) instead of a collapsed card
    with a lone apology. Keys strip only promises chords that work with no
    player (volume, mute, esc); play/seek chords wait until one exists.
    Next: the user wants it more playful — this item is still open.
- [ ] Notifications rehaul
- [ ] Sidepanel — simplified mode, and expand-mode changes
- [ ] Keybind previewer rehaul
